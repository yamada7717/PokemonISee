<div class="container mx-auto max-w-md p-6 bg-white mt-[124px]">
  <h2 class="text-xl font-bold text-gray-900">パスワードリセット再設定メールを送る</h2>
  <%= form_with(url: password_resets_path, method: :post, local: true) do |form| %>
    <div class="mt-4">
      <%= form.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-700" %>
      <%= form.email_field :email, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm", placeholder: "メールアドレスを入力してください" %>
      <p class="mt-2 text-xs text-gray-500">PokemonISeeに登録されているメールアドレスのみ送信可能です。</p>
    </div>
    <div class="mt-6 flex items-center justify-end gap-x-6">
      <%= form.submit "送信", class: "rounded-md bg-blue-500 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-blue-700" %>
    </div>
  <% end %>
</div>
