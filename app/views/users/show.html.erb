<div class="container flex-grow mx-auto p-6 md:flex mt-24">
  <div class="flex flex-col items-center mb-6 md:mb-0 md:mr-6 mr-2 bg-white p-4 shadow rounded-lg h-[250px]">
    <p class="text-xl font-bold mb-1"> <%= @user.name %></p>
    <p class="text-sm text-gray-600 mb-2"><%= @user.introduction %></p>
    <div class="flex space-x-4 text-sm mb-2">
      <%# ここにフォローカウント・リンクを後で設置 %>
    </div>
    <%# ここにフォロー機能を設置 %>
  </div>
</div>
